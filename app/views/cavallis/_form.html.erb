<%= form_with(model: cavalli) do |form| %>
  <% if cavalli.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(cavalli.errors.count, "error") %> prohibited this cavalli from being saved:</h2>

      <ul>
        <% cavalli.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nome %>
    <%= form.text_field :nome, class:'form-control bg-dark mioform ' %>
  </div>

  <div class="field">
    <%= form.label :descrizione %>
    <%= form.text_field :descrizione, class:'form-control bg-dark mioform ' %>
  </div>

  <div class="field">
    <%= form.label :età %>
    <%= form.number_field :età, class:'form-control bg-dark mioform ' %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
